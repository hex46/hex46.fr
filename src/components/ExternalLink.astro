---
import Link from "./Link.astro";

interface Props {
  title: string;
  href: string;
  text: string
}

const { title, href , text} = Astro.props;

const relList = ["noreferrer", "external"]
---

<Link href={href} rel={relList} title={title} text={text} external/>